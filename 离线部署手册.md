# ServiceHub 离线部署手册

适用版本：本文所在 Git 提交。目标是无互联网环境部署完整前端、后端和 MySQL 数据库。本文不包含真实账号、密码、令牌、证书或内网地址。

## 1. 先选部署模式

| 模式 | 用途 | 入口 | 重要边界 |
| --- | --- | --- | --- |
| 单机离线验收 | 明天功能测试、演示、验收 | `http://127.0.0.1:8088` | 使用 `mysql,local-dev`、本地测试账号、关闭真实病毒扫描；仅绑定本机，禁止暴露到局域网或生产 |
| 正式内网部署 | 集团内实际使用 | 由受管 HTTPS 反向代理发布 | 使用 `mysql,prod,clamav`、本地账号密码、外部 MySQL、受管 ClamAV 和密钥平台；IAM/OIDC 后续可选接入 |

当前自定义 BPMN 与多表单设计仍为 `DRAFT_ONLY`，可以设计和保存，不能作为自由流程发布运行。既有受控工单生命周期可以正常测试。

## 2. 版本和资源要求

- Docker Engine 24+，Docker Compose v2；验收机建议至少 4 核、8 GiB 内存、20 GiB 可用空间。
- 构建基线：JDK 21、Maven 3.9、Node.js 24、MySQL 8、Flowable 7.2.0。
- 离线验收需要 3 个镜像：`servicehub-backend`、`servicehub-frontend`、MySQL 8。
- 正式环境另外需要已审批的 MySQL、ClamAV、HTTPS 反向代理和备份存储。当前可使用本地账号，不要求先接 IAM。
- 所有服务器时间同步到统一时源，应用/数据库按 UTC 保存，展示端按用户时区显示。

部署前记录并核对：Git 提交号、三个镜像 ID/仓库摘要、镜像包 SHA-256、MySQL 小版本、Flowable 版本、当前最高 Flyway 版本（本版为 V50）、操作者和变更单号。

## 3. 在有网络的构建机制作离线包

### 3.1 先验证源码

在仓库根目录执行：

```bash
cd frontend
npm ci --ignore-scripts
npm test
npm run build

cd ../backend
mvn -B test
cd ..
```

本次提交前基线：前端 42/42、后端 145/145，生产构建通过。离线包必须来自相同提交且重新通过验证。

### 3.2 用批准的不可变基础镜像构建

先从可信仓库取得并记录以下基础镜像的完整摘要：

- Maven 3.9 + Eclipse Temurin JDK 21 Alpine 构建镜像；
- Eclipse Temurin JRE 21 Alpine 运行镜像；
- Node.js 24 Alpine 构建镜像；
- `nginxinc/nginx-unprivileged` Alpine 运行镜像；
- MySQL 8 运行镜像。

不要在正式制品中使用 `latest` 或未记录摘要的浮动标签。示例中的 `<...@sha256:...>` 必须替换为实际批准值：

```bash
docker build --pull=false -f backend/Dockerfile \
  --build-arg SERVICEHUB_BUILD_IMAGE=<approved-maven-image@sha256:digest> \
  --build-arg SERVICEHUB_RUNTIME_IMAGE=<approved-jre-image@sha256:digest> \
  -t servicehub/backend:20260904 .

docker build --pull=false -f frontend/Dockerfile \
  --build-arg SERVICEHUB_NODE_BUILD_IMAGE=<approved-node-image@sha256:digest> \
  --build-arg SERVICEHUB_NGINX_IMAGE=<approved-nginx-unprivileged-image@sha256:digest> \
  -t servicehub/frontend:20260904 .

docker tag <approved-mysql-image@sha256:digest> servicehub/mysql:approved
```

构建完成后确认镜像中没有 `.env`、源码临时目录、测试截图或本地日志：

```bash
docker image inspect servicehub/backend:20260904 --format '{{.Id}}'
docker image inspect servicehub/frontend:20260904 --format '{{.Id}}'
docker history --no-trunc servicehub/backend:20260904
docker history --no-trunc servicehub/frontend:20260904
```

### 3.3 导出和校验

```bash
mkdir -p offline-bundle
docker save -o offline-bundle/servicehub-images.tar \
  servicehub/backend:20260904 servicehub/frontend:20260904 servicehub/mysql:approved

cp devops/docker-compose.offline-test.yml offline-bundle/
cp devops/docker-compose.production.yml offline-bundle/
cp devops/.env.offline-test.example offline-bundle/
cp devops/.env.production.example offline-bundle/
cp -R devops/database-baseline offline-bundle/
cp devops/backup-mysql.sh devops/restore-mysql.sh offline-bundle/
cp 离线部署手册.md offline-bundle/

sha256sum offline-bundle/servicehub-images.tar > offline-bundle/SHA256SUMS
find offline-bundle -type f ! -name SHA256SUMS -print0 | sort -z | xargs -0 sha256sum >> offline-bundle/SHA256SUMS
```

将离线包、`SHA256SUMS` 和 Git 提交号通过批准介质送入离线区。不要携带构建机的 Maven/npm 缓存、Docker 登录配置、SSH 密钥或真实 `.env`。

Windows 构建机可用 `Get-FileHash -Algorithm SHA256` 生成/复核镜像包哈希；最终清单仍须由交付双方签字或进入制品系统。

## 4. 明天单机离线验收

### 4.1 导入镜像

在离线验收机：

```bash
cd offline-bundle
sha256sum -c SHA256SUMS
docker load -i servicehub-images.tar

docker image inspect servicehub/backend:20260904 --format '{{.Id}}'
docker image inspect servicehub/frontend:20260904 --format '{{.Id}}'
docker image inspect servicehub/mysql:approved --format '{{.Id}}'
```

把输出的 `sha256:...` 镜像 ID 填入私有环境文件；也可使用已导入的固定标签，但不得重新指向其他镜像。

### 4.2 创建测试环境文件

```bash
cp .env.offline-test.example /run/servicehub/offline-test.env
chmod 600 /run/servicehub/offline-test.env
```

填写：三个镜像 ID、两个随机数据库密码、至少 32 字节的游标签名密钥和 HMAC 密钥。可在受控终端生成随机值，生成后立即写入权限为 `0600` 的文件，不粘贴到聊天、工单或命令历史。

测试入口默认绑定 `127.0.0.1:8088`。如需让另一台机器访问，不能直接把 local-dev 端口改为 `0.0.0.0`；应改用正式部署模式和 HTTPS 登录入口。

### 4.3 第一次启动

必须从空的 Docker named volume 开始。Compose 会按顺序：初始化 MySQL → 导入业务 V34 基线 → 导入 Flowable 7.2.0 结构 → 导入合成演示数据 → Flyway 登记 V34 并前向迁移 V35–V50 → 启动前后端。

```bash
docker compose --env-file /run/servicehub/offline-test.env \
  -f docker-compose.offline-test.yml config --quiet

docker compose --env-file /run/servicehub/offline-test.env \
  -f docker-compose.offline-test.yml up -d

docker compose --env-file /run/servicehub/offline-test.env \
  -f docker-compose.offline-test.yml ps
```

查看启动状态时不要输出环境变量：

```bash
docker compose -f docker-compose.offline-test.yml logs --tail=200 servicehub-mysql
docker compose -f docker-compose.offline-test.yml logs --tail=200 servicehub-backend
docker compose -f docker-compose.offline-test.yml logs --tail=100 servicehub-frontend
```

打开 `http://127.0.0.1:8088`。验收环境会创建 `requester`、`ops01`–`ops05` 和 `admin`；密码统一取私有环境文件中的 `SERVICEHUB_TEST_LOCAL_ACCOUNT_PASSWORD`，仓库没有可用于生产的默认密码。可测试：普通用户提单/知识、5 人候选抢单、领取后候选收敛、运维系统范围、账号管理、系统目录、BPMN 预览和表单设计。

### 4.4 数据库核对

进入 MySQL 容器时通过交互或容器环境取凭据，不把密码写在命令行：

```bash
docker compose --env-file /run/servicehub/offline-test.env \
  -f docker-compose.offline-test.yml exec servicehub-mysql sh
```

容器内核对：

```bash
MYSQL_PWD="$MYSQL_PASSWORD" mysql -u servicehub servicehub -e \
  "SELECT version, success FROM flyway_schema_history ORDER BY installed_rank DESC LIMIT 5;"

MYSQL_PWD="$MYSQL_PASSWORD" mysql -u servicehub servicehub -e \
  "SELECT COUNT(*) AS systems FROM service_system_registry; SELECT COUNT(*) AS tickets FROM ticket;"
```

最高成功迁移应为 `50`，`success` 应为 `1`。`ACT_`、`FLW_`、`IDM_` 表应存在。测试期间不要手工修改业务状态、Flowable 表或 `flyway_schema_history`。

V46 增加本地账号；V47 增加个人工单草稿；V48 增加处理表单，并退役旧版内置的三个通用受理/解决/关闭审批兜底策略；V49 增加知识收藏与草稿状态。升级后普通受理/解决/关闭直接执行，另行配置的匹配审批策略仍生效。升级前核对审批策略并做好备份。草稿按账号隔离，暂存不会创建流程；附件仍需重新选择。

### 4.5 停止与清空

保留测试数据停止：

```bash
docker compose --env-file /run/servicehub/offline-test.env \
  -f docker-compose.offline-test.yml down
```

以下命令会永久删除这个 Compose 项目的测试数据库、附件和临时卷，只能在确认项目名和目标为验收环境后执行：

```bash
docker compose --env-file /run/servicehub/offline-test.env \
  -f docker-compose.offline-test.yml down -v
```

## 5. 正式内网部署

### 5.1 网络和目录

在部署平台预创建受控 Docker 网络，并确保只有以下必要路径可达：前端→后端、后端→MySQL、后端→ClamAV。后续启用 OIDC 时再增加回调→IAM 路径。`SERVICEHUB_INTERNAL_NETWORK` 填该网络名。数据库和 ClamAV 若不在 Docker 内，网络团队必须显式提供路由和防火墙白名单。

```bash
install -d -m 0750 /srv/servicehub
install -d -m 0700 /srv/servicehub/attachments /srv/servicehub/tmp /srv/backup/servicehub
install -d -m 0700 /run/servicehub
```

正式容器默认只绑定 loopback，由批准的 HTTPS 反向代理发布 `SERVICEHUB_FRONTEND_PORT`。代理必须保留 `Host`、`X-Forwarded-Proto=https`、客户端地址和请求 ID，限制请求体不超过 11 MiB，并为 `/oauth2/`、`/login/`、`/api/` 和 SPA 路由提供同一外部域名。

### 5.2 MySQL 账号和最小权限

使用 MySQL 8、`utf8mb4`、UTC。DBA 创建空库和两个独立账号；下面只表示授权范围，账号来源主机和密码由 DBA/密钥平台替换，完成后的 SQL 不进入仓库：

```sql
CREATE DATABASE servicehub
  CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;

-- 迁移账号：仅 ServiceHub 目标库的结构维护，不授予全局或 GRANT 权限。
GRANT ALL PRIVILEGES ON servicehub.* TO 'servicehub_migration'@'<approved-app-subnet>';

-- 运行账号：只做业务 DML。
GRANT SELECT, INSERT, UPDATE, DELETE ON servicehub.*
  TO 'servicehub_runtime'@'<approved-app-subnet>';
```

正式环境不导入 `servicehub-v26-demo-data.sql`。

### 5.3 全新生产库初始化

1. 只对全新空库导入 `database-baseline/servicehub-v34-schema.sql`。
2. 导入 `database-baseline/flowable-7.2.0-mysql-schema.sql`。
3. 首次应用启动临时设置：`SERVICEHUB_FLYWAY_BASELINE_ON_MIGRATE=true`、`SERVICEHUB_FLYWAY_BASELINE_VERSION=34`。
4. Flyway 登记 V34 后只前向执行 V35–V50；核对成功后立即把 `BASELINE_ON_MIGRATE` 改回 `false` 并重新创建后端容器。
5. 后续任何环境都禁止再次开启基线开关，禁止 `flyway clean`。

已有数据库升级时绝不导入基线。先备份并在隔离恢复库演练，然后使用迁移账号正常执行既有 Flyway 历史后的前向迁移。

V50 为同一工单、动作及来源版本的待审批请求增加唯一约束，防止升级后重复受理或绕过既有审批。迁移前执行下列只读检查，结果应为空；如有记录，先保留审计并处置重复活动审批，不得删历史或强行 repair 跳过。

```sql
SELECT ticket_id, action_code, source_ticket_version, source_workflow_version, COUNT(*) AS active_count
FROM ticket_lifecycle_action_approval_request
WHERE status IN ('PENDING_APPROVAL', 'EXPIRING')
GROUP BY ticket_id, action_code, source_ticket_version, source_workflow_version
HAVING COUNT(*) > 1;
```

Flowable 表独立管理，生产配置 `flowable.database-schema-update=false`。Flowable 升级必须使用与依赖版本严格匹配、评审过的 DDL，不能依赖普通应用启动自动改表。

### 5.4 正式环境文件

```bash
cp .env.production.example /run/servicehub/production.env
chmod 600 /run/servicehub/production.env
```

必须填写：前后端镜像摘要、受控网络、运行/迁移数据库账号、附件路径、ClamAV 精确允许地址、HTTPS 门户来源、每环境独立的 cursor/HMAC 密钥，以及一次性本地管理员引导账号。首次启动且账号表为空时同时提供以下五项，创建成功后从运行环境移除引导密码并重建容器：

```text
SERVICEHUB_LOCAL_AUTH_BOOTSTRAP_LOGIN
SERVICEHUB_LOCAL_AUTH_BOOTSTRAP_PASSWORD
SERVICEHUB_LOCAL_AUTH_BOOTSTRAP_DISPLAY_NAME
SERVICEHUB_LOCAL_AUTH_BOOTSTRAP_ORGANIZATION_ID
SERVICEHUB_LOCAL_AUTH_BOOTSTRAP_ORGANIZATION_NAME
```

当前设置 `SERVICEHUB_LOCAL_AUTH_ENABLED=true`、`SERVICEHUB_IAM_SSO_ENABLED=false`。后续启用 OIDC 时，通过受控 Compose override 注入 Spring Security client registration；OIDC issuer 必须精确匹配受信租户，redirect URI 为：

```text
https://<servicehub-domain>/login/oauth2/code/servicehub
```

本地账号密码使用 bcrypt、自适应失败锁定、服务端 Session 和 CSRF；不要把浏览器密码当作未来系统间 API 密钥。没有 ClamAV 或数据库备份时不得接入真实用户和附件。

### 5.5 启动和验收

```bash
docker compose --env-file /run/servicehub/production.env \
  -f docker-compose.production.yml config --quiet

docker compose --env-file /run/servicehub/production.env \
  -f docker-compose.production.yml up -d

docker compose --env-file /run/servicehub/production.env \
  -f docker-compose.production.yml ps
```

正式验收至少包括：

- 未登录访问业务 API 返回 401；账号密码登录后会话更换且 CSRF 生效；连续失败触发锁定；跨组织/系统对象返回 403/404。
- 普通用户只能提单、读取自己的工单和已发布知识；运维账号只处理负责系统；共享队列 5 人候选在领取后只剩实际受理人。
- 使用 `ACTUATOR_VIEW` 专用身份读取 readiness，状态为 UP；普通用户不能读取 actuator。
- 新建事件工单、受理、处理、待用户反馈、关闭；历史流程版本不会被最新设计替换。
- 系统→服务→表单选择正确；无映射、停用、跨组织服务不可直接调用接口绕过。
- 附件 EICAR 测试串被隔离；ClamAV 断连/超时关闭式失败，未扫描文件不可下载或预览。
- `flyway_schema_history` 最高版本为 46 且无失败，Flowable 表版本与 7.2.0 一致。
- 重启前后工单、审计、设计草稿和附件仍存在；日志中无密码、令牌、Cookie、HMAC 或附件正文。

## 6. 备份、恢复和升级

仓库提供 `backup-mysql.sh` 和 `restore-mysql.sh`。密码只放在仓库外、权限为 `0600` 的 MySQL client defaults 文件：

```ini
[client]
host=db.internal.example
user=servicehub_backup
password=REPLACE_FROM_SECRET_MANAGER
ssl-mode=VERIFY_IDENTITY
ssl-ca=/run/servicehub/mysql-ca.pem
```

备份：

```bash
chmod 700 backup-mysql.sh restore-mysql.sh
SERVICEHUB_MYSQL_CLIENT_DEFAULTS_FILE=/run/servicehub/backup.cnf \
SERVICEHUB_DB_NAME=servicehub \
SERVICEHUB_BACKUP_DIR=/srv/backup/servicehub \
./backup-mysql.sh
```

备份后记录文件 SHA-256、大小、时间、MySQL 版本、Flyway 最高版本、GTID/binlog 位点（如启用）并复制到受控异地介质。

恢复只允许到明确命名的隔离库：

```bash
SERVICEHUB_MYSQL_CLIENT_DEFAULTS_FILE=/run/servicehub/restore.cnf \
SERVICEHUB_RESTORE_DATABASE=servicehub_restore_drill_20260904 \
SERVICEHUB_BACKUP_FILE=/srv/backup/servicehub/servicehub-servicehub-<timestamp>.sql.gz \
./restore-mysql.sh
```

恢复演练检查：业务表数量、Flyway 历史、`ACT_*` 运行/历史数据、本地账号（只核对数量/状态，不导出哈希）、审计追加链、附件文件与数据库引用、关键工单读写。恢复库不得连接生产消息、外部身份接口或真实通知通道。

升级步骤固定为：备份 → 隔离恢复 → 新版本迁移演练 → 兼容性/时长评审 → 摘流量 → 生产迁移 → 应用滚动替换 → 验收。应用镜像回退不等于数据库回退；数据库已前向迁移时优先使用前向兼容补丁，只有 DBA 批准且恢复演练有效时才能整库恢复。

## 7. 故障排查

| 现象 | 检查 |
| --- | --- |
| 前端 502 | 后端容器是否启动、同一 Docker 网络内能否解析 `servicehub-backend`、后端日志是否数据库连接失败 |
| 后端启动失败 | MySQL/ClamAV/本地账号引导配置、Flyway 校验和、Flowable 表是否已导入、密钥长度、附件目录权限 |
| 页面一直 401 | 本地账号是否启用、账号是否停用/锁定、Session Cookie/CSRF、代理 `X-Forwarded-Proto`；后续 OIDC 再检查 client registration |
| 新库报表已存在 | 错误地把 V34 基线导入后未做一次性 Flyway baseline；停止部署，不要删表或改历史，重新创建空库 |
| 找不到系统/服务 | 账号组织与负责系统范围、系统发布状态、服务发布状态、系统/模块启用映射 |
| 附件不能使用 | ClamAV 健康/病毒库、允许主机、附件卷和临时卷权限；禁止关闭扫描器后继续生产测试 |
| 重启后数据丢失 | 是否误用无卷容器、named volume/绑定目录是否变化、MySQL 是否连接到错误实例 |

收集故障材料时仅保留请求 ID、时间、状态码、脱敏资源 ID和容器状态；不得导出完整环境变量、数据库密码、Cookie、OIDC token、HMAC、附件原文或用户敏感信息。
