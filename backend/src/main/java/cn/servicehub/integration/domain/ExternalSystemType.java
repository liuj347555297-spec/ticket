package cn.servicehub.integration.domain;

/** External systems are integrated through explicitly configured, least-privilege adapters. */
public enum ExternalSystemType {
    CMDB, MONITORING, LOG_PLATFORM, APM
}
