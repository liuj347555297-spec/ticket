package cn.servicehub.integration.application;

/** Intentionally mapped to a generic 403 response: callback failures reveal no verification detail. */
public class IntegrationSecurityException extends RuntimeException { }
